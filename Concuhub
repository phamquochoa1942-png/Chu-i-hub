_G.AutoAttackAll = true
task.spawn(function()
local RS = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local Plr = Players.LocalPlayer
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local AttackRemotes = ReplicatedStorage:WaitForChild("Modules"):WaitForChild("Net"):WaitForChild("RE/RegisterHit")

local isClicKing = true
task.spawn(function()
        while _G.FastAttackAll do
            if IsActive then
                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
            end
            task.wait(0.001)
        end
    end)
    
    while _G.FastAttackAll do
        local Character = LocalPlayer.Character
        if not Character then
            if IsActive then ForceReset() end
            task.wait(0.2)
            continue
        end
    local args = {
                    TargetPart,
                    {},
                    [4] = "079996f9"
                }
                
                pcall(function()
                    AttackRemote:FireServer(unpack(args))
                end)
            end
        else
            if IsActive then ForceReset() end
        end
        
        task.wait()
    end
end) 
 
