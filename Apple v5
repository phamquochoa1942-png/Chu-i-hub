local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local Plr = Players.LocalPlayer
Local Char = Plr.Character or Plr.CharacterAdded:Wait()
local RegisterAttack =game:GetService("ReplicatedStorage").Modules.Net:FindFirstChild("RE/RegisterAttack")
local function danhnhanhvl()
pcall(function()
 RegisterAttack:FireServer(0.5)
 for i, v in pairs(game.Workspace.Enemies:GetChildren() do
 if v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 then
 local magnitude = (v.HumanoidRootPart.Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
 if magnitude < 60 then 
 RegisterAttack:FireServer(v.HumanoidRootPart)
               end 
           end
       end
   end)
end
_G.FastAttack = true
task.spawn(function()
while _G.FastAttack do
   danhnhanhvl()
 task.wait(0.1)
     end
end
