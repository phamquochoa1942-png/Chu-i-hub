local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "🍎 Apple Hub | V5 Banana Killer (GOD)",
    SubTitle = "By Quoc Hoa",
    TabWidth = 160, Size = UDim2.fromOffset(580, 460), Acrylic = true, Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl 
})

-- [[ HỆ THỐNG BIẾN ĐIỀU KHIỂN ]]
_G.AutoFarm = false
_G.AutoChest = false
_G.BringMob = true
_G.FastAttack = true
_G.AutoStats = false
_G.StatPoint = "Melee"
_G.SelectWeapon = "Melee"
_G.AutoRaid = false
_G.InfiniteDash = false

-- [[ TỔNG HỢP CÁC TAB ]]
local Tabs = {
    Main = Window:AddTab({ Title = "🏠 Farm Tổng", Icon = "home" }),
    Combat = Window:AddTab({ Title = "⚔️ Chiến Đấu", Icon = "swords" }),
    Raid = Window:AddTab({ Title = "⚡ Raids", Icon = "zap" }),
    Items = Window:AddTab({ Title = "💎 Đồ Hiếm", Icon = "gem" }),
    Stats = Window:AddTab({ Title = "📊 Chỉ Số", Icon = "bar-chart" }),
    Misc = Window:AddTab({ Title = "⚙️ Khác", Icon = "settings" })
}

-- --- TAB FARM TỔNG (FIX DỨT ĐIỂM GIẬT NÚT) ---
Tabs.Main:AddSection("Cày Cấp & Tiền")

local FarmToggle = Tabs.Main:AddToggle("AutoFarm", {Title = "Auto Farm Level (Xịn)", Default = false})
FarmToggle:OnChanged(function()
    _G.AutoFarm = FarmToggle.Value
end)

local ChestToggle = Tabs.Main:AddToggle("AutoChest", {Title = "Auto Farm Rương", Default = false})
ChestToggle:OnChanged(function()
    _G.AutoChest = ChestToggle.Value
end)

-- --- TAB CHIẾN ĐẤU ---
Tabs.Combat:AddSection("Hỗ Trợ Chiến Đấu")
local BringMobToggle = Tabs.Combat:AddToggle("BringMob", {Title = "Gom Quái (Bring Mob)", Default = true})
BringMobToggle:OnChanged(function()
    _G.BringMob = BringMobToggle.Value
end)

local FastAttackToggle = Tabs.Combat:AddToggle("FastAttack", {Title = "Fast Attack Pro", Default = true})
FastAttackToggle:OnChanged(function()
    _G.FastAttack = FastAttackToggle.Value
end)

Tabs.Combat:AddDropdown("WeaponSelect", {
    Title = "Vũ Khí Đang Dùng",
    Values = {"Melee", "Sword", "Blox Fruit"},
    Default = "Melee",
    Callback = function(Value) _G.SelectWeapon = Value end
})

-- --- TAB RAIDS ---
Tabs.Raid:AddSection("Tự Động Đi Raid")
local RaidToggle = Tabs.Raid:AddToggle("AutoRaid", {Title = "Auto Start & Kill Raid", Default = false})
RaidToggle:OnChanged(function()
    _G.AutoRaid = RaidToggle.Value
end)

Tabs.Raid:AddButton({
    Title = "Mua Chip (Mystery Scientist)",
    Callback = function() game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("RaidsEntity","BuyChip") end
})

-- --- TAB VẬT PHẨM ---
Tabs.Items:AddSection("Săn Đồ Ngon")
Tabs.Items:AddButton({
    Title = "Auto Nhặt Trái Ác Quỷ (Fruit)",
    Callback = function()
        for i,v in pairs(game:GetService("Workspace"):GetChildren()) do
            if v:IsA("Tool") and v.Name:find("Fruit") then
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.Handle.CFrame
            end
        end
    end
})

-- --- TAB CHỈ SỐ ---
Tabs.Stats:AddSection("Tự Động Cộng Điểm")
local AutoStatsToggle = Tabs.Stats:AddToggle("AutoStats", {Title = "Kích Hoạt Auto Stats", Default = false})
AutoStatsToggle:OnChanged(function() _G.AutoStats = AutoStatsToggle.Value end)

Tabs.Stats:AddDropdown("StatPoint", {
    Title = "Cộng Vào:", Values = {"Melee", "Defense", "Sword", "Demon Fruit"}, Default = "Melee",
    Callback = function(Value) _G.StatPoint = Value end
})

-- --- TAB MISC ---
Tabs.Misc:AddSection("Tiện Ích Người Chơi")
local DashToggle = Tabs.Misc:AddToggle("InfDash", {Title = "Vô Hạn Dash (Lướt)", Default = false})
DashToggle:OnChanged(function() _G.InfiniteDash = DashToggle.Value end)

Tabs.Misc:AddSlider("WalkSpeed", {Title = "Tốc Độ Chạy", Default = 20, Min = 20, Max = 300, Rounding = 0, Callback = function(V) 
    if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = V 
    end
end})

Tabs.Misc:AddButton({Title = "Đổi Server (Server Hop)", Callback = function() game:GetService("TeleportService"):Teleport(game.PlaceId, game.Players.LocalPlayer) end})

-- [[ LOGIC VẬN HÀNH TÁCH BIỆT - CHỐNG XUNG ĐỘT UI ]]

-- Logic Farm riêng (Thread ngắt cứng)
task.spawn(function()
    while true do
        if _G.AutoFarm then
            pcall(function()
                -- Ngắt tức thì ngay khi biến đổi sang false
                if not _G.AutoFarm then return end
                
                game:GetService("VirtualUser"):CaptureController()
                game:GetService("VirtualUser"):ClickButton1(Vector2.new(50, 50))
                
                if _G.BringMob then
                    for _, v in pairs(game:GetService("Workspace").Enemies:GetChildren()) do
                        -- Kiểm tra cực nhanh để dừng loop bring mob
                        if not _G.AutoFarm then break end
                        if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                            v.HumanoidRootPart.CanCollide = false
                            v.HumanoidRootPart.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -5)
                            v.HumanoidRootPart.Velocity = Vector3.new(0,0,0)
                        end
                    end
                end
            end)
        end
        task.wait(0.01) -- Đảm bảo không bị giật lag luồng
    end
end)

-- Logic Rương riêng
task.spawn(function()
    while true do
        if _G.AutoChest then
            for i,v in pairs(game:GetService("Workspace"):GetChildren()) do
                if not _G.AutoChest then break end 
                if v.Name:find("Chest") then
                    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.CFrame
                    task.wait(0.15) 
                end
            end
        end
        task.wait(0.1)
    end
end)

-- Logic Dash & Stats
task.spawn(function()
    while true do
        if _G.InfiniteDash then
            pcall(function()
                debug.setupvalue(getrenv()._G.Characters.Dash, 3, 0)
            end)
        end
        task.wait(0.1)
    end
end)

-- [[ NÚT TRÒN 🍎 ]]
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local ToggleButton = Instance.new("TextButton", ScreenGui)
ToggleButton.Size = UDim2.new(0, 50, 0, 50)
ToggleButton.Position = UDim2.new(0, 20, 0, 200)
ToggleButton.Text = "🍎"
ToggleButton.TextSize = 30
ToggleButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
ToggleButton.Draggable = true 
local UICorner = Instance.new("UICorner", ToggleButton)
UICorner.CornerRadius = UDim.new(1, 0)

ToggleButton.MouseButton1Click:Connect(function()
    game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.LeftControl, false, game)
    task.wait(0.01)
    game:GetService("VirtualInputManager"):SendKeyEvent(false, Enum.KeyCode.LeftControl, false, game)
end)

Window:SelectTab(1)
Fluent:Notify({Title = "Apple Hub V5", Content = "Đã FIX lỗi giật nút Farm cho chủ tịch Quoc Hoa!", Duration = 5})
