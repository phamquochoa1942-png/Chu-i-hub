_G.AutoAttack = true

local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local Plr = Players.LocalPlayer
local RegisterAttack = game:GetService("ReplicatedStorage").Modules.Net:FindFirstChild("RE/RegisterAttack")
local function EquipWeapon()
local Char = Plr.Character
if not Char then return nil
 end
local hum = Char:FindFirstChild("Humanoid")
local tool = Char:FindFirstChildOfClass("Tool") or Plr.Backpack:FindFirstChildOfClass("Tool")
  if tool and then
  if tool.Parent ~= Char
if tool and hum:FindFirstChild(tool.Name) == nil then
   hum:EquipTool(tool)
   end
   return tool
   end
   return nil
   end
 task.spawn(function()
   while _G.AutoAttack do
local Weapon = EquipWeapon()
if Weapon then
local args = {
    [1] = 0.5
}
pcall(RegisterAttack.FireServer,RegisterAttack(0.5)
    end)
end
    task.wait(0.10)
   end
 end)
