local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "🍎 Apple Hub | Supreme V5",
    SubTitle = "by Bánh mì kẹp thịt",
    TabWidth = 160, Size = UDim2.fromOffset(580, 460), Acrylic = true, Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl 
})

-- [[ TẠO NÚT TRÒN BAY LƠ LỬNG (FLOATING BUTTON) ]]
local FloatingButton = Instance.new("TextButton")
FloatingButton.Name = "FloatingButton"
FloatingButton.Parent = game:GetService("CoreGui"):FindFirstChild("RobloxGui") or game:GetService("CoreGui"):FindFirstChild("GuiService")
FloatingButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0) -- Màu Đỏ (Bố có thể đổi màu ở đây)
FloatingButton.Position = UDim2.new(0, 50, 0, 50) -- Vị trí ban đầu
FloatingButton.Size = UDim2.new(0, 50, 0, 50) -- Kích thước (50x50)
FloatingButton.Font = Enum.Font.SourceSansBold
FloatingButton.Text = "🍎" -- Hình hiện trên nút (Chủ tịch có thể thay bằng 🍏)
FloatingButton.TextColor3 = Color3.fromRGB(255, 255, 255)
FloatingButton.TextSize = 30
FloatingButton.ZIndex = 1000

-- Làm cho nút tròn có thể kéo lê
local Dragging = false
local DragInput, DragStart, StartPosition
FloatingButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        Dragging = true
        DragStart = input.Position
        StartPosition = FloatingButton.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then Dragging = false end
        end)
    end
end)
FloatingButton.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then DragInput = input end
end)
game:GetService("UserInputService").InputChanged:Connect(function(input)
    if input == DragInput and Dragging then
        local Delta = input.Position - DragStart
        FloatingButton.Position = UDim2.new(StartPosition.X.Scale, StartPosition.X.Offset + Delta.X, StartPosition.Y.Scale, StartPosition.Y.Offset + Delta.Y)
    end
end)

-- Chức năng ẩn/hiện menu khi bấm vào nút tròn
FloatingButton.MouseButton1Click:Connect(function()
    Fluent:ToggleWindow() -- Hàm này sẽ tự động ẩn/hiện menu
end)

-- [[ TẠO CÁC TAB (GIỐNG HỆT BẢN TRƯỚC) ]]
local Tabs = {
    Main = Window:AddTab({ Title = "🏠 Tự Động", Icon = "home" }),
    Sea = Window:AddTab({ Title = "🌊 Sự Kiện Biển", Icon = "waves" }),
    Raid = Window:AddTab({ Title = "⚡ Auto Raid", Icon = "zap" }),
    Stats = Window:AddTab({ Title = "📊 Chỉ Số", Icon = "bar-chart" }),
    Misc = Window:AddTab({ Title = "⚙️ Hệ Thống", Icon = "settings" })
}

-- TAB CHÍNH
Tabs.Main:AddSection("Auto Farm Level")
Tabs.Main:AddToggle("FarmLvl", {Title = "Bật Auto Farm", Default = false})
Tabs.Main:AddDropdown("Weapon", {
    Title = "Chọn Vũ Khí",
    Values = {"Melee", "Sword", "Fruit"},
    Default = "Melee",
    Callback = function(v) _G.Weapon = v end
})

-- TAB BIỂN
Tabs.Sea:AddSection("Săn Quái Biển")
Tabs.Sea:AddToggle("AutoSB", {Title = "Auto Sea Beast", Default = false})
Tabs.Sea:AddToggle("AutoTerror", {Title = "Auto Terror Shark", Default = false})

-- TAB HỆ THỐNG
Tabs.Misc:AddSection("Treo Máy")
Tabs.Misc:AddToggle("WhiteScreen", {Title = "Màn Hình Trắng", Default = false}):OnChanged(function() game:GetService("RunService"):Set3dRenderingEnabled(not Fluent.Options.WhiteScreen.Value) end)

Window:SelectTab(1)
Fluent:Notify({Title = "Apple Hub", Content = "Đã nạp đầy đủ chức năng và Nút Tròn!", Duration = 5})
